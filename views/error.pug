extends layout.pug

block content
    nav.grid-container.portfolio-breadcrumb
      a(href='/') ← Back

    #error

      h2 Oh no! You're not suppose to be here!

      h3 Error message:
      p #{error.message}

      h3 Error status:
      p #{error.status}

      h3 Error stack:
      p #{error.stack}
